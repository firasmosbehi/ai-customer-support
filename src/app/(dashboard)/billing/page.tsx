import { PagePlaceholder } from "@/components/dashboard/PagePlaceholder";

export default function BillingPage() {
  return (
    <PagePlaceholder
      title="Billing"
      description="Stripe checkout, portal, and usage controls are implemented in Phase 5."
    />
  );
}
